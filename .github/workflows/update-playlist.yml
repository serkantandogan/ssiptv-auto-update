name: Update SS IPTV playlist

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Build playlist
        run: python scripts/update_playlist.py

      - name: Commit playlist changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update SS IPTV playlist"
          file_pattern: "public/ssiptv.m3u public/validation-report.json"

